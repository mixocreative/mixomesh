# MIXOMESH — Legacy Agent Context

`AGENTS.md` is the active instruction authority for current
Codex sessions. Use `Blueprint.md` as the canonical rebuild spec and
`README.md` for the current run/verify commands.

This file remains only as a compatibility pointer for tools that still open
`CLAUDE.md`; do not duplicate project rules or phase checklists here.
