# MIXOMESH Build Log

Append-only build history, split out of Blueprint.md PART 15 (arch review
A10) so the canonical spec stops accumulating stale narrative. Newest
entries go at the bottom of the relevant section.

### Completed Phases

- **Phase 1 — Foundation:** events, state/history/input managers, scene bootstrap, icon registry, toast/status UI, layout shell. Milestone: empty viewport, MMB orbit, axes + grid, status bar live, `Ctrl+Z` registered.
- **Phase 2 — Asset Pipeline:** AssetLoader, ShaderLibrary registration stub, MeshValidator, AssetPanel. Milestone: mount directory, drop GLB, see thumbnail, get validation toast.
- **Phase 3 — Selection & Interaction:** selection model, Babylon gizmos, Outliner, ContextMenu, Properties transforms/source unit, viewport shortcuts. Milestone: click-select, modal transform with snapping, grouping, frame, undo.
- **Phase 4 — Shader System (closed 2026-05-14):** full ShaderLibrary, ShaderPanel, shader/UV Properties sections, merge-strategy modal, imported texture readback, right-panel splitter, LMB horizontal-plane drag. Deferred at close: copy-from-active, user-swatch persistence, multi-material-per-mesh, sub-section collapse persistence.
- **Phase 5 — Print Pipeline (closed and Chrome-verified 2026-05-15):** PrintManager, PrintPanel, pre-export validation gate, bed preview, OBJ+MTL export, STL/3MF groundwork, collections, working-ratio re-bake, transform baking, scale lock, viewport toolbar, nav cube, CAD mouse remap. Deferred at close: nav cube corner/edge snaps, deeper camera-follow testing, old v3.1 scalar `scene.gridSize` styling migration.

### Later Build History

- **Persistence & Export Hardening (closed 2026-05-17):** PersistenceManager save/open/new/recent/autosave/relink, embedded `.mixo` asset bytes + live relink tiers, ghost UI, Smart Replace, Transform Swab, import-transform normalization, structured export pipeline, non-destructive export clone prep, progress overlay, axis/winding switches for 3MF.
- **Mimaki Textured 3MF (closed 2026-05-18):** printer-driven 3MF dispatch, `_build3MFModelMaterialsExt`, 3MF Materials Extension loader path, per-vertex UV emission, OPC texture parts, per-part relationships/content types, `weldSolidOnly`, texture dedup, solid fallback via colorgroup, mixed textured/solid packages.
- **Post-Mimaki polish wave (2026-05-18):** filename system with unique names/ratio suffix/save picker/inline rename, OBJ solid-colour PNG synthesis (4×4 RGBA, dedup by RRGGBBAA, alpha in PNG + MTL `d`), Properties copy-from-active buttons for Transform / Shader / UV Override, split-on-import validation UI refinements.
- **Vite-only cleanup (2026-06-08):** removed the legacy root runtime (`main.js`, `index.vite.html`, root `core/`, `ui/`, `config/`, `styles/`, and `scripts/serve.mjs`). `index.html` is now the Vite shell, app code/data live under `src/`, tests import from `src/`, and the verified command set is `npm run typecheck`, `npm run build`, `npm run test`, `npm run test:browser`.

- **Review remediation wave (2026-06-11):** deep code review (31 findings) + Blueprint architecture review; fixed 4 critical (blank texture export via Promise readPixels, reload texture loss, delete-undo pivot corruption, dead group validation) + safety/hygiene bundles; texture identity contract (10b, .mixo 3.2), position-based dirty, validation result cache (A6) with Outliner badges + export warning gate; module split pass (commands/, assets/, print/ writers, scene/ outline+bed); functional Chrome export smoke (`npm run test:export`); TS runtime mirrors deleted (A7); autosave embed-skip (A9). 19 headless test files.
- **Field-report + UX wave (2026-06-12):** fixed all 8 first-eyeball findings (viewport distortion via ResizeObserver, dead selection, wireframe-edge overlay clones, OBJ+MTL sibling resolution + reload rebind, editable Size, panel CSS) plus follow-ups: import progress overlay with real byte progress, wheel-scrub on number inputs, worker-based OBJ parse (Babylon loader in a NullEngine worker — no UI freeze, main-thread fallback), B5 validation-toast click-through, CameraRig + PivotSession extracted from SceneManager (993 → 389/455/263), export smoke pins texture Y-flip + 3MF re-import + OBJ worker path.
- **UI overhaul wave (2026-06-12):** Blender-inspired control language (neutral-gray tokens, inset-pill fields, right-aligned labels, sentence-case headers, segmented tabs — styles/blender.css loaded last); viewport toggles under the NavCube (wireframe edges + colour, matte/flat) replacing the Print Preview tab; Scene panel (grid, render: background light/dark + exposure/contrast/shadows/light intensities, camera: FOV + near clip — persisted in sceneSettings.render); FOUR workspaces (Layout/Shading/Scene/Print, Ctrl+Shift+1..4, icon pill) each showing Properties + at most one specialist section with task-filtered Properties sections; tri-state panelCollapsed fix (concrete false force-showed panels and erased workspace differences, localStorage v2 migration); ShaderPanel.focus auto-switches to Shading; per-workspace scroll memory + task-aware empty hints; scripts/ui-screenshot.mjs headless capture harness with PROBE mode (caught the focus-hijack and the AppShell boot flex-pin bugs).

- **Rendering output wave (2026-06-13):** Scene ▸ Render gained grade controls (tone map ACES/Neutral-KHR/Standard/Off, saturation via colorCurves, vignette toggle + weight); new Scene ▸ Rendering section (`state.scene.renderOut`, persisted): resolution presets + custom W×H, Render-view compose toggle (parks nav pose, jumps to stored render pose, ui/RenderFrame.js aspect-frame overlay with outside darkening, Set view), Export PNG via RTT screenshot (selection silhouette skipped for free, furniture hidden, transparent variant disables the bg Layer + alpha-0 clearColor), turntable video (full 360° alpha sweep, wall-clock eased, Esc cancel, hidden-tab cancel, mp4 avc3 preferred with auto-retry to WebM vp8 on empty result) — core/RenderOutput.js + pure core/render/RenderMath.js (unit-tested: easing, format pick, frame fit, filename stem shared with §12 export contract). Browser smoke pins transparent/opaque capture alpha + frame overlay; `npm run test:video` is a HEADED check because MediaRecorder hard-freezes the renderer in headless Chromium — and on the dev machine Chrome 149.0.7827 freezes the same way even live (trivial 2D-canvas recording, user profile — Chrome-build bug; Edge 149 records a 1.7 MB mp4 fine, `VIDEO_CHECK_EDGE=1`).
- **Turntable rework + Chrome video fix (2026-06-13, second wave):** sweep semantics locked — camera never moves or pans: re-aim once at the world vertical axis (`setTarget((0, targetHeight, 0))`), then only alpha sweeps 360° around the pinned pivot; key/fill lights + future environmentTexture rotate by the same angle (`_sweepRig`, shared preview/record). Video export now works on Chrome 149: primary path is an offline WebCodecs encoder — frame-by-frame RTT renders at the exact output resolution, H.264 VideoEncoder + mp4-muxer (new dependency) into in-memory mp4, deterministic, furniture hidden, loops cleanly (last frame just short of 360°); MediaRecorder (which hard-crashes Chrome 149 and all headless Chromium — STATUS_BREAKPOINT, while WebCodecs is unaffected per tests/webcodecs-probe.mjs) is now only the no-WebCodecs fallback. Browser smoke records a real 1 s mp4 headless + pins no-pan/origin-circle sweep invariants; Chrome-149 confirm guard removed (no longer needed).
- **HDRI environment lighting (2026-06-13):** Scene ▸ Environment gains an "HDRI lighting" sub-group — default ON (preset Studio, intensity 0.6), toggleable, presets Studio/Neutral/Outdoor. Three prefiltered .env cube textures (Babylon CDN asset copies) bundled in public/env/, loaded via CubeTexture.CreateFromPrefilteredData into scene.environmentTexture; lighting only (no skybox — the gradient backdrop stays), drives IBL on PBR materials (every imported model). Texture cached across toggle, disposed on preset change; intensity = scene.environmentIntensity. Turntable rotates the HDRI with the camera: rotationY sign (+δ) determined EMPIRICALLY by a new browser-smoke mirror-sphere probe — mid-sweep capture must match baseline while a camera-only rotation must not (the analytic-light-derived −δ guess counter-rotated the env and failed the probe; flipped and pinned). CubeTexture added to the boot.ts namespace.- **Audit remediation + effects wave (2026-06-13, third wave):** full UX/code audit adopted. Code: SceneManager re-split under its 450 budget (599 → 440) — scene/EnvironmentRig.js (lights/RENDERONCE shadows/floor/HDRI + HDRI_STATUS load toasts + idle preset prefetch) and scene/EdgeOverlay.js extracted; shadow casters tracked by uniqueId Set; shadow map now RENDERONCE + invalidateShadows() (caster world-matrix observables, setting changes, sweep light rotation) killing the constant 2048² ESM per-frame cost; PROJECT_NEW/LOADED abort in-flight previews/recordings WITHOUT restoring the stale camera. UX: exports (PNG + video) shoot from the stored render pose when one exists (free nav restored after), Ctrl+Alt+E export hotkey, rendering action buttons disable while busy. Perf: offline encoder drops the per-frame PNG round-trip — manual outputRenderTarget render into one reused RTT + readPixels + row-flip → VideoFrame(RGBA) (captureFrameRGBA() is the smoke probe); mp4-muxer is now a lazy dynamic-import chunk; a @babylonjs manualChunks vendor split was MEASURED and rejected (rolldown tree-shaking loss: 7.3 MB vs 4.2 MB — warning comment in vite.config.ts). Features: cross-section "Cut view" (scene/ViewEffects.js — per-content-mesh scene.clipPlane via render observables, print-space axis/offset/flip, session-only state, cuts appear in exports, shadows stay uncut), SSAO contact darkening (SSAO2 pipeline, default ON, strength 0–2, viewport-only, feature-detected with dispose-on-toggle; coexists with the silhouette post-process), bounce-in import pop (scene/ImportBounce.js — 260 ms easeOutBack on ASSET_INSTANTIATED, exact-scaling restore, reduced-motion aware, loads don't bounce by construction). Smoke additions pin all of it: frame-source pixel variance, section cut/flip alpha triple, bounce landing, soft-shadow-with-caster (RENDERONCE tripwire), SSAO toggle, recording abort. Bug found by smoke: a [data-sec] row selector collided with the <section data-sec> collapse wrappers (change events bubble → panel re-render → detached nodes); section rows renamed data-sect*, warning documented in Blueprint. 26 headless test files + browser smoke + export smoke green; SceneManager 440/450, EnvironmentRig 243/250, CameraRig budget raised to 550 (optics + pose APIs).
- **SSAO import-freeze fix (2026-06-13, follow-up):** two ~80k-tri GLBs with a 4096² base-colour texture froze on import. Root cause = the SSAO2 pipeline shipped default-ON the same day: its geometry prePass re-renders ALL scene geometry into MRT (color/normal/depth) targets every frame, pushing worst-frame from ~24 ms to ~250–330 ms on those meshes (measured in SwiftShader) — recurring stalls that read as a frozen import and can wedge a TDR-prone GPU/driver (this machine's Chrome 149). Fix: SSAO now defaults OFF (still toggleable in Scene ▸ Environment ▸ Ambient occlusion); the custom selection-mask RTT also sets `noPrePassRenderer = true` so the prePass never double-renders geometry into it. Verified: both models import at ~20 ms worst-frame with SSAO off; browser smoke still exercises the SSAO enable/disable toggle.
- **Heavy-asset perf wave (2026-06-13, follow-up to SSAO fix):** profiled the real workflow (lots of 4096²-textured, 80k+ tri print models) with a headless import benchmark. Findings + fixes: (1) **Validation was the one true main-thread block on import** — ~450 ms of weld + non-manifold edge-map per 80k-tri mesh, run as a microtask. Moved topology to a Web Worker (`workers/MeshValidate.worker.js` + `ValidateWorker.js` client; `MeshValidator._topology` posts a copy of positions/indices, awaits counts; inline fallback for no-Worker). Worker uses numeric packed keys (no string churn) and **signed-volume inverted-normals** replacing the old 64-ray O(tris)-per-ray heuristic. Measured ~450 ms → ~12 ms wall, UI never blocks. (2) **Selection-outline pass + mask RTT ran every frame even with nothing selected** (64-tap fullscreen pass, real cost at 4K) — now detached when the selection is empty, re-attached on first selection (`SelectionOutline._setOutlineEnabled`; browser smoke pins it). (3) **Adaptive resolution** (`scene/AdaptiveResolution.js`): replaced raw `adaptToDeviceRatio` with a capped effective-DPR (≤1.5) base + a safety-valve dynamic downscale (rolling avg frame-time > 45 ms sheds pixels, < 28 ms recovers) — cuts fragment cost for high-DPI users on heavy textured scenes; exports use their own RTT size so output quality is unaffected. Deliberately NOT done: silent texture downscale (export reads pixels from the live GPU texture → would break the LOCKED Mimaki full-res requirement) and static-mesh/material freezing (fights the gizmo + ShaderLibrary edit paths; CPU isn't the bottleneck at realistic mesh counts — GPU/VRAM is). Note: VRAM for N unique 4096² textures is bounded by the export-fidelity requirement; a safe viewport texture cap needs the export path to re-read from source bytes first (future work). All headless tests + browser smoke + export smoke green.
- **Texture cap + Cross Section + toggle audit (2026-06-13, follow-up wave):** (1) **Viewport texture cap backed by export-from-source-bytes** — the one safe lever left for large texture counts. `assets/TextureSource.js` freezes each texture's full-res export PNG ONCE at the registration seam (`captureAndCap`, before any downscale), captured via the SAME `textureToPngBlob` the export uses so it is orientation-identical. `assets/TextureCap.js` then downscales the GPU copy via `Texture.updateURL` (re-decoded from source — raising the cap restores detail) and `ExportTextures.textureToBlob(tex, assetId)` returns the captured source verbatim when present, so Mimaki exports stay bit-for-bit full-res regardless of the viewport cap. `scene.render.textureCapPx` (0=off default; 4096/2048/1024) lives under Scene ▸ Environment ▸ Performance; changing it calls `AssetLoader.recapAllTextures`. New unit test `texture-source.test.mjs` (first-writer-wins, export-prefers-source, GPU fallback); export Y-flip smoke confirms the source path is byte-correct. (2) **"Section" → "Cross Section"** rename + a Fusion-360-style visual: a semi-transparent, diagonally-striped indicator quad (`scene/ViewEffects.js`) drawn at the cut so the user sees where/which-axis the cut is. Carries no `metadata.meshId` → auto-excluded from clipping/shadows/selection-mask; `RenderOutput._hideFurniture` hides it during PNG/video capture (geometric cut still renders, striped overlay doesn't). Browser smoke pins the indicator exists when on / disposed when off, and that it never changes the cut alpha. (3) **Checkbox → toggle-button audit:** boolean feature/mode/visibility switches became pressable `.pp-toggle` buttons (blender.css §8c) — Scene HDRI/Vignette/Floor/Shadows/SSAO/Grid/Axes/Cut view/Render view, Properties Visible(`Eye`)/Locked(`Lock`)/print-part, Print show-bed-volume; sub-options (Transparent PNG, Ease, Flip, export scope, bake-solid) stay checkboxes. Fixed a latent bug found en route: `PropertiesPanel.js` used `sectionIcon` without importing it (ReferenceError in `_render`). New `Gauge` icon. Typecheck + build + 27 unit + browser smoke + export smoke green.
- **In-browser perf harvest — WebGPU backend (2026-06-13, follow-up wave):** brainstormed a native-shell pivot (Electron/Tauri) and concluded most of the perf/stability win is free *in-browser*; chose to harvest it without changing stack. Findings: (a) **WebCodecs video was already shipped** (the offline `_recordOffline` H.264 path; MediaRecorder is only the fallback) — no work needed. (b) **WebGPU engine** added: `SceneManager.init` is now async, `_createEngine` prefers `BABYLON.WebGPUEngine` (whole attempt — support probe + `initAsync` — raced against an 8 s timeout, any failure falls back to `new BABYLON.Engine`); `isWebGPU()` + `window.__MX_ENGINE` diagnostic. The one custom shader (selection outline) got a **WGSL twin** registered in `ShaderStore.ShadersStoreWGSL`, picked by `engine.isWebGPU`; `WebGPUEngine`/`ShaderLanguage`/`ShaderStore` added to the boot namespace. New `test:webgpu` (`browser-webgpu-check.mjs`) verifies — head-ful, `WEBGPU_HEADFUL=1` — that WebGPU boots, the WGSL shader compiles + renders with zero device errors, and the loop runs (32 frames). **WebGPU is OPT-IN only (`?engine=webgpu`), WebGL stays the default**, because Babylon 9.6.2's offline render-target → `readPixels` path returns EMPTY on WebGPU (`rawCenter [0,0,0,0]`) — and that path backs ALL capture (PNG / turntable / thumbnail), which carries the Mimaki full-res LOCK. Defaulting to WebGPU would silently break export; flip the default in `_tryWebGPU` once the capture path is fixed. **(Capture FIXED same session — see next entry.)** (c) **KTX2 GPU-compressed textures: deferred** — runtime PNG→Basis needs the Basis *encoder* wasm (~MBs + multi-second per-4096² CPU stall), its VRAM win only materializes with a compressed-texture GPU path (i.e. WebGPU, now opt-in/capture-blocked), and the existing texture cap already delivers the VRAM lever for heavy scenes. Poor cost/benefit now; revisit with the WebGPU default flip. Typecheck + build + 27 unit + browser smoke + export smoke + head-ful WebGPU check all green; WebGL default path unchanged.
- **WebGPU capture blocker FIXED (2026-06-13, same session):** root-caused the empty WebGPU capture — a manual out-of-loop `scene.render()` never **submits** the WebGPU command buffer (WebGPU batches + submits at frame boundaries; WebGL executes eagerly), so `readPixels` read nothing. Confirmed by a head-ful strategy probe: rendering through the frame pipeline, `rt.render()` + `engine.flushFramebuffer()`, and the canvas screenshot all returned real pixels. Fix: `RenderOutput._renderSceneToTarget` now calls `engine.flushFramebuffer?.()` after the manual render (fixes offline video on WebGPU; harmless on WebGL). `capturePng` branches by `engine.isWebGPU` — WebGL keeps the proven `CreateScreenshotUsingRenderTargetAsync` (the naive manual transparent path regressed its alpha-coverage), WebGPU uses a manual render→flush→readPixels→`OffscreenCanvas`-encode path (`_capturePngWebGPU`). The project thumbnail (`PersistenceManager._screenshot`) now routes through `capturePng` so it works on both engines too. Verified head-ful (`WEBGPU_HEADFUL=1 npm run test:webgpu`, now hard-asserting): video frame distinct 805, opaque PNG 105, outline PNG 169, **orientation matches the displayed image, transparent PNG center alpha 255 / corner 0** — all capture correct on WebGPU. WebGPU is now fully functional; kept OPT-IN (`?engine=webgpu`) only pending verification on more GPUs/drivers — flipping the default is a one-liner. Print export stays engine-independent (source bytes + mesh data, no GPU readback) so the Mimaki LOCK is untouched either way. WebGL default path unchanged; all suites green.
- **Import error modal + refinements wave (2026-06-13, same session):** (Import error handling) failed model/texture imports now open a detail MODAL (`src/ui/ImportError.js` — filename + plain-language hint keyed off the message + a collapsible `<details>` technical stack) instead of a transient toast. `safeImport(fn, filename)` is a `safeAsync` variant routing the catch to the modal; both import entry points use it — `ViewportDrop` (OS drop + Session/folder drag) and `AssetPanel._activateCard` (double-click). Registered in `main.ts` via `ImportError.init()`; styled in print.css; browser smoke pins it (synthetic failure → modal shows filename/message/details → closes; the smoke ignores the one intentional `console.error`). (Refinements 1–3) **#1 WebGPU export readback VERIFIED safe** — the export fallback (`ExportTextures.textureToBlob` → `TextureReadback.textureToPngBlob`) reads STATIC uploaded textures, which submit on upload, so the RTT-only flush bug doesn't apply; `test:webgpu` now asserts a known-content DynamicTexture reads back non-empty (no code change needed). **#2 CDP harness DRY'd** — extracted `tests/cdp-harness.mjs` (Cdp, evaluate, waitFor, freePort, findBrowser, launch/teardown helpers), imported by both `browser-smoke.mjs` and `browser-webgpu-check.mjs` (~150 lines of duplication removed; both re-verified green). **#3 WebGPU bundle lazy-load measured + REJECTED** — boot chunk is 4222.81 KB with the static `WebGPUEngine` import vs 4222.80 KB without (~0 delta: the WebGPUEngine class is thin and its bulk is already pulled in by core code the WebGL path imports), so lazy-loading would save nothing. Engine-pref refinement also landed earlier this session: `_preferredEngine()` reads `?engine=` (URL wins) then `localStorage.mxEngine`, else WebGL. Typecheck + build + 27 unit + browser smoke + export smoke + head-ful WebGPU all green.
- **Cross-section cap + round floor + error/loading centralization (2026-06-13, same session, brainstormed):** (1) **Cross-section solid cap** — the striped indicator is now a real Fusion-style CAP that colours ONLY the solid interior cross-section, done with the STENCIL buffer and NO extra geometry pass (heavy-asset perf): while a cut is on, every clipped content material renders with `stencil` INVERT (even-odd parity → set inside the solid, clear outside; exact for watertight meshes), and a cap quad at the plane draws only where stencil != 0. (2) **Cut-plane border** — a thin accent `LinesMesh` rectangle at the plane extent so the plane is visible even where the cut misses the solid; shows cut-view is active. Both viewport-only (`RenderOutput._hideFurniture` hides them + drops the stencil writes during capture; the geometric cut still renders). `Constants` added to the boot namespace for the stencil ops. (3) **Round floor disc + diameter** — `CreateGround` → `CreateDisc` (unit disc radius 0.5 SCALED to `floorDiameterMM`, so resizing never rebuilds; 0 = auto 4× bed); new `scene.render.floorDiameterMM` + Scene-panel input; backward-safe (old saves → auto). (4) **Centralized error/loading** — new `src/ui/Status.js` owns the two genuine cross-cutting concerns: `reportError` (toast, or detail modal for import/export), `guard` (run + route failure), `runTask` (one loading path — overlay or loading toast, always cleaned up), `safeAsync` (toast adapter = guard). `safeAsync` moved here (re-exported from `Toast.js` for compat); `ImportError.safeImport` = `guard({modal:true})`. DELIBERATELY left alone (false-DRY): intentional silent fallbacks + domain loaders with staged/nested progress (AssetLoader import overlay, PrintManager export). Scope brainstormed first (user weighed aggressive-vs-lean → lean-but-complete: centralize the shared concerns thoroughly, leave the rest). Browser smoke extended: floor-is-disc + diameter scaling, cut-plane border exists-on/gone-off; cap PIXELS stay a live eyeball (stencil unreliable in SwiftShader + cap is viewport-only/hidden in capture). Blueprint fully synced. typecheck + build + 27 unit + browser smoke + export smoke + head-ful WebGPU all green.
- **Cross-section solid cap + offset slider (2026-06-13, same session, refinement):** the cap was semi-transparent + `disableDepthWrite` → you saw THROUGH it into the hollow / back faces. Now OPAQUE (stripe texture has no alpha) + depth-write ON: the cap sits at the cut plane (frontmost after clipping) so it OCCLUDES the hollow interior / back faces behind it — the cut reads as a solid machined cross-section (Fusion look). Still stencil-masked to the real solid cross-section (non-crossed area stays empty), border kept, viewport-only (hidden in exports), and STILL no extra geometry pass (perf unchanged). Note: semi-transparent is impossible-by-physics for "hide the hollow" — a see-through face reveals what it covers; opaque is required. Offset control changed from a number input to a **range SLIDER** spanning the content extent along the axis (lowest..highest point) via new `ViewEffects.getSectionExtentMM(axis)` → `{minMM,maxMM,hasContent}` (re-exported through SceneManager; reuses the cap's `_contentBounds`, no extra cost); slider drives the cut live on `input` (cheap), axis select re-renders so the range follows. Browser smoke pins the extent API; CSS for the range + value readout in properties.css. Blueprint synced. typecheck + build + 27 unit + browser smoke + export smoke + head-ful WebGPU all green.
- **Cross-section stencil cap → back-face fill (2026-06-13, same session):** the stencil cap still showed the hollow back-face shell on the user's GPU. Root cause: the single-pass `stencil` INVERT during the normal color render is DEPTH-GATED (`opStencilDepthPass` only fires for the frontmost fragment), so the even-odd parity never accumulates → the mask is wrong → the cap quad doesn't draw → you see the clipped interior directly. And it's GPU-dependent. Brainstormed options (A shader / B stencil-done-right / C CSG2-real-geometry); user chose **inspection-only, reliable preview (B′ back-face fill)**. Replaced the stencil cap with: per clipped solid, a shared-geometry CLONE (parent=null + baked source world matrix + no metadata.meshId → auto-excluded from clip/shadow/mask) renders the source's BACK faces with a flat opaque striped unlit material (front faces culled via flipped `sideOrientation`), so the interior fills with a solid cap colour — NO stencil, reliable on any GPU, shares geometry (no RAM dup), own clip observers, idempotent per mesh, disposed on cut-off. Border kept. All stencil code removed (`_setMeshStencil`/`_setAllStencil`/`material.stencil`). Documented for the print tool: a REAL cut face for export = geometry (CSG2, `PrintManager._csgRebake` already uses CSG2) — a future "apply cut" intersects the solid with the half-space; the viewport fill is preview-only. Smoke updated (fill-clone exists-on/gone-off via `mx-section-cap-*`). CAVEAT: the `sideOrientation` flip assumes CCW-front (standard imports) — if a model renders inverted, flip the one constant; cap visual is a live eyeball (hidden in capture). typecheck + build + 27 unit + browser smoke + export + head-ful WebGPU all green.
- **Cross-section translucent amber + offset buffer + final audit (2026-06-13, same session):** (1) offset slider range padded ±10 mm (`getSectionExtentMM`) so the plane fully clears the model. (2) Cross-section fill restyled to theme accent **#f59e0b**: semi-transparent body (α0.30) with denser, less-transparent stripes (α0.78), alpha-blended + depth-write off (was opaque). Same back-face-clone mechanism. (3) FINAL CHECK: code integrity (typecheck + build + 27 unit + browser smoke + export + head-ful WebGPU all green), function-logic review of ViewEffects clone lifecycle (shared geometry ref-counted, safe dispose order, prune-on-dispose, live `_plane` via observers, idempotent), and UI audit via `node scripts/ui-screenshot.mjs` (4 workspaces captured, no console errors, panels + amber accent render correctly across Layout/Shade/Scene/Print). Blueprint fully synced.
- **MediaRecorder removed + cross-section stripe/colour fix (2026-06-13, same session):** (1) **Dropped the MediaRecorder video fallback** — it hard-freezes Chrome 149 + all headless Chromium (never a safe fallback). `recordTurntable` is WebCodecs-only now; no `VideoEncoder` ⇒ a clear "needs Chrome/Edge" error, not a hung tab. Removed `_recordOnce` + the `pickVideoFormat` import. (2) **Cross-section fill stripe/colour fix** — the back-face clone sampled the stripe texture by the MODEL's UVs (no visible diagonals on textured / UV-less meshes) and α0.30 washed the amber out. Now: hue is a constant amber EMISSIVE (`#f59e0b`, exact regardless of blending), and a PLANAR-projected texture supplies only the stripe ALPHA (body α0.45 + denser stripes α0.92, `uScale/vScale=6`) → uniform diagonals in world space. typecheck + build + 27 unit + browser smoke (WebCodecs 1 s mp4 headless) + export + head-ful WebGPU all green.
- **Inspection borrows (Rodin-style) + cap stripe retry (2026-06-13, same session):** evaluated Rodin/Hyper3D viewer; borrowed the print-relevant inspection bits (skipped AI-gen + LOD which fight full-res print). Built: (1) **Display-mode selector** in `ViewportToggles` — mutually-exclusive Shaded / Matte / Base Color `<select>` (folds the old matte toggle in). Base Color = flat albedo (Mimaki ink preview): `SceneManager._setBaseColorMode` sets PBR `unlit` / Standard `disableLighting`+emissive, store/restore per material like print-preview (viewport-only, export-safe). New `scene.overlays.baseColorView`; `_setMode` drives both overlays so modes stay exclusive; re-applied on `ASSET_INSTANTIATED`. (2) **Live mesh-stats HUD** — `src/ui/MeshStats.js` writes `△ tris · W×D×H mm · watertight` into the status-bar centre on selection (reads validation cache for watertight; zero new chrome). (3) **Cross-section stripe retry** — switched the cap stripe texture to PROJECTION (screen-space) coords + `uScale/vScale=28` (PLANAR/EXPLICIT showed nothing on textured/UV-less meshes). DEFERRED (reported, need per-mesh-clone / texture-swap + export+perf care): inverted/back-face highlight, UV-checker. Smoke pins base-color unlit flip/restore + 3-option mode selector; UI audit (`ui-screenshot.mjs`) confirms the mode selector renders, no console errors. typecheck + build + 27 unit + browser smoke + export + head-ful WebGPU all green.
- **Cross-section stripe (CustomMaterial) + resin grey + inverted-face (2026-06-13, same session):** (1) **Cross-section stripe — finally working.** Texture-coords (EXPLICIT/PLANAR/PROJECTION) never drove the stripe alpha on textured/UV-less meshes. Moved to a `CustomMaterial` (StandardMaterial subclass → clip-plane + alpha for free) whose `Fragment_Before_FragColor` computes diagonal stripes PER-FRAGMENT from `vPositionW` (world pos, ~4 mm period, body α0.32 / stripe α0.92), amber emissive. No texture, no UVs. (CustomMaterial emits GLSL → WebGPU stripe injection needs transpile; acceptable, WebGPU opt-in. `CustomMaterial` added to boot namespace.) (2) **Resin-grey default.** `AssetLoader._applyResinDefault` recolours UNTEXTURED + near-white materials to grey 0.62 (PBR albedoColor / Standard diffuseColor) after `registerFromContainer`, so raw STL/OBJ/default-white-glTF imports read like grey/ED-resin prints; textured/coloured left alone. (3) **Inverted/back-face highlight** (the deferred Rodin borrow) — `scene/BackfaceCheck.js`: a shared-geometry red back-face clone per content solid (front culled via flipped sideOrientation, no meshId → auto-excluded from clip/shadow/mask/export), so any RED from outside = a hole/inverted face. New overlay `invertedFaces` (state default + boot loop + OverlayKey + setOverlay JSDoc + `AlertTriangle` toggle in ViewportToggles); re-applied on ASSET_INSTANTIATED/PROJECT_LOADED when on. Smoke pins inverted on→clones / off→gone. typecheck + build + 27 unit + browser smoke + export + head-ful WebGPU all green; UI audit clean.
- **UV-checker mode (2026-06-13, same session):** the 4th display mode — `SceneManager._setUvCheckerMode` swaps each CONTENT material's base texture (PBR albedoTexture / Standard diffuseTexture) for a shared 8×8 grey checker with amber grid lines (`_ensureCheckerTexture`, wrap-mode), mapped by the mesh's own UVs so density/stretching/seams show (flat result = no UVs). Store/restore per material like base-color/matte → viewport-only + export-safe (export reads the frozen TextureSource, not the checker; export smoke still green). Skips furniture + section/back-face viz clones + non-content meshes. New overlay `uvCheckerView` (state default + boot loop + OverlayKey + setOverlay JSDoc + the `<select>`'s 4th option; `_setMode` now drives all three mode overlays exclusively); re-applied on ASSET_INSTANTIATED. Smoke pins the texture swap/restore + 4-option selector. All inspection modes (Shaded/Matte/Base Color/UV Checker) + inverted-face overlay now done; only AI-gen/LOD intentionally skipped. typecheck + build + 27 unit + browser smoke + export + head-ful WebGPU all green.
- **Resin-grey for material-less meshes (2026-06-13, same session):** `_applyResinDefault` only touched meshes that HAVE a material (registerFromContainer iterates `container.materials`, so STL/no-shader/missing meshes have none → skipped → rendered stark white via the default). Fix: `SceneManager.init` sets `scene.defaultMaterial` to a matte medium-light grey (0.72) — Babylon renders all material-less meshes with it → STL / missing / ghost now read like grey/ED-resin prints. Bumped `_applyResinDefault` grey 0.62 → 0.72 to match (medium-light). Covers both has-material-but-white and no-material cases. Build + smoke green.
- **Resin-grey for STL — explicit material assign (2026-06-13, follow-up):** STL still rendered white — `scene.defaultMaterial` alone wasn't reliably picked up for material-less meshes. Fix: `_applyResinDefault` now ASSIGNS a shared matte grey `StandardMaterial` (`_resinGrey`, 0.72, specular 0) to any content mesh with NO material (was: skipped), in addition to recolouring untextured-white materials; `scene.defaultMaterial` grey kept as fallback. Smoke pins the default-material grey. Build + smoke + export green.
- **Resin-grey redesign — shaderless-only, never override imports (2026-06-13, follow-up):** STL STILL white — the assign-grey had a `getTotalVertices()` guard that returns 0 on container meshes BEFORE `addAllToScene`, so STL was skipped. Per user direction (only shaderless gets our grey; NEVER override authored materials): (1) moved `_applyResinDefault` to AFTER `addAllToScene` (geometry bound), guard now `!mesh.geometry`; (2) REMOVED the near-white recolour branch entirely — imported materials are never touched, white or not; (3) it ONLY assigns the shared `_resinGrey` to meshes with NO material. Shaderless FACES still fall through to the greyed `scene.defaultMaterial`. Roles: defaultMaterial = render-time fallback for material-less FACES; `_applyResinDefault` = explicit grey for material-less OBJECTS at import. Build + smoke + export green.
- **Resin-grey value 0.72 → 0.5 (2026-06-13, follow-up):** STL "still white" turned out NOT to be a logic bug — a smoke probe that imports a real ASCII STL proved the mesh DOES get `mx-resin-grey` (StandardMaterial, [0.72,0.72,0.72]). The 0.72 albedo just WASHES to near-white under the bright 3-light studio + ACES + exposure. Lowered `_resinGrey` + `scene.defaultMaterial` to 0.5 so it reads as a clear medium grey once lit. Browser smoke now imports an STL and ASSERTS the mesh gets the grey at 0.5 (permanent regression guard). Build + smoke green.
- **Unify resin-grey to one material (2026-06-13, follow-up):** per request, both cases now point to ONE material — `scene.defaultMaterial`. Removed the separate `_resinGrey` `StandardMaterial`; `_applyResinDefault` now assigns `SceneManager.getScene().defaultMaterial` (the same instance greyed in init) to material-less meshes. Shaderless FACES already use it (render fallback) + shaderless OBJECTS get it explicitly → one place to tune the grey (`SceneManager.init`, 0.5). Smoke asserts the imported STL's material IS `scene.defaultMaterial`. Build + smoke + export green.
- **3D-cursor toolkit + two-tone outline + copy/paste chooser (2026-06-14):** Blender-borrowed batch. (1) **3D cursor upgrade** — extracted to `scene/Cursor3D.js`: kept the yellow translucent ball, added a billboarded crosshair + dashed ring at constant on-screen size (`camera.radius × SCREEN_K`, `renderingGroupId 1` so never occluded). `setCursor` now writes `state.scene.cursor3d` (silent) + fires `CURSOR_CHANGED`; restored on load via `setCursorFromState`. (2) **Snap ops** (`core/CursorTools.js`) — Selection → Cursor (rigid median translate, one undoable `TransformCommand`), Cursor → Selection (median), Cursor → World Origin; cursor moves stay out of undo (Blender-parity). (3) **N-panel** (`ui/CursorPanel.js`) — slide-out viewport sidebar on **Shift+N** (plain N is the right-panel toggle), 3D-Cursor tab with live mm XYZ inputs + snap buttons + "Use Cursor as Pivot"; clipped by `#viewport overflow:hidden` so closed = 26 px tab. (4) **Two-tone selection outline** — `SelectionOutline` mask went TWO-CHANNEL (active emissive R, selected G); shader tints R→`#c2410c` (darker orange), G→`#f59e0b` (amber), active over selected. GLSL + WGSL twins both updated; removed `MASK_BRIGHTNESS_*`, added `OUTLINE_ACTIVE_HEX`/`OUTLINE_SELECTED_HEX`. (5) **Copy/Paste aspect chooser** (`ui/CopyPaste.js`) — Ctrl+C → popup (Object/Location/Rotation/Scale/Loc+Rot/All) stores active's data; Ctrl+V → filtered popup applies to all selected as one undoable step ("Object" = duplicate). Multi-select copy-active→others falls out for free. Browser smoke gained 6 asserts (cursor ball+ring meshes, setCursor→state sync, two-channel mask R/G, N-panel XYZ inputs mounted). typecheck + build + 27 unit + browser smoke green; UI screenshots clean. Live eyeball pending (N-panel open + cursor visual + active-darker-orange against other selected).
- **Multi-control state-sync audit (2026-06-14, follow-up):** swept all state with >1 UI control for the silent-write drift that hit pivot-mode. Found one more: `gizmo.space` (toolbar World/Local button + the `` ` ``/`~` toggle hotkey) — `setGizmoSpace` wrote state silently, so the hotkey left the toolbar button stale. Fixed with `EVENTS.GIZMO_CHANGED` dispatched from `PivotSession.setGizmoMode`+`setGizmoSpace`; ViewportToolbar subscribes it. Rest verified clean: `followMode` toolbar-only; overlays partitioned one-control-each (modes/wireframe/inverted=ViewportToggles, grid/axes=ScenePanel, bedPreview=PrintPanel); cursor visibility now also synced on `CURSOR_CHANGED` (covers Shift+RMB placement updating the N-panel Show/Hide button). Blueprint PART 7 documents the rule: any state with >1 control MUST dispatch an event. Smoke gained `pivotSync` + `gizmoSpaceSync` asserts. typecheck + build + 27 unit + browser smoke green.
- **Box / marquee multi-select (2026-06-14):** LMB-drag across empty viewport space sweeps a rectangle; every content mesh whose bounding-box centre projects inside is selected, Shift+drag is additive, a <3 px drag falls back to a plain empty click (clears), Esc/RMB cancels. `core/BoxSelect.js` splits a pure screen-rect hit-test (`meshIdsInRect`/`normRect`/`projectToScreen`) from the DOM overlay (`.box-select` <div>, never touches the RTT → exports byte-identical). Starts only on an EMPTY left-down so it never competes with body-drag (left-down on a mesh) or camera nav (camera ignores the left button); the G/R/S modal guard suppresses it during a transform. Wired into InputManager's pointer observable. User REJECTED a Blender-style `B` force-box-mode (keep the gesture stateless). 8 unit asserts (rect normalisation, meshId-chain filter, pickable/hidden culls, submesh dedup, behind-camera cull) + browser smoke (live projection finds a real mesh; synthetic left-drag creates the overlay without throwing). Headless green. Commit eaca62d.
- **Per-user settings persistence + reset + header logo (2026-06-14):** the browser remembers last-used PANEL settings across sessions; object/content is never persisted as settings. `src/config/default-settings.json` is the editable single source of truth for factory values — consumed by `StateManager.INITIAL_STATE` (spread into the slices) AND by every reset, so changing defaults is a JSON edit. `core/SettingsStore.js` owns a curated SCHEMA (render, renderOut minus session `pose`, grid, display overlays grid/axes/printPreview only, print, gizmo space+snap, pivotMode); pure core (`pickSettings`/`mergeSettings`/`parseStored`/`applySectionToState`/`factoryState`) + glue (`load`/`save` debounced/`seedBootState`/`applyToScene`/`resetSection`/`resetAll`). `mergeSettings` narrows to known keys so a corrupt/old blob can't inject unknown paths. **Precedence FILE WINS:** `seedBootState` merges persisted over factory at state init + on New (`PersistenceManager.newProject`); opening a `.mixo` restores the file's slices directly and never re-seeds. Save fires on USER edits only (capturing change/input on `#rp-scene-body`+`#rp-print-body`, plus `GIZMO_CHANGED`/`PIVOT_MODE_CHANGED`) — boot/load programmatic applies bypass it. Reset: per-section ↺ on the Scene panel sections + one print ↺ on the Print tab bar (`resetSection`), top-bar ↺ in ProjectMenu (`resetAll`, settings only — leaves workspace/widths/section-collapse); both restore from the config file, re-apply via `applyToScene`, dispatch `SETTINGS_RESET`. **Scene-protected (fix 0f15fe7):** with objects loaded, reset PRESERVES `print.workingRatio`+`targetRatio` (`SCENE_PROTECTED`/`_guardFactory`) — workingRatio rebakes meshes, targetRatio drives export/bed-fit, so resetting under loaded content would silently rescale; empty scene/New resets them. Header logo `public/logo.svg` → `#app-logo` far-left of `#header`. Phase 6 (persistence milestone) removed/assumed-done this session. 13 unit asserts incl. a pin that `INITIAL_STATE` equals `default-settings.json` (locks config-as-truth) + browser smoke (logo renders, edit persists with content excluded, resetSection restore+re-persist, resetAll clears key + ratio-guard under a loaded mesh). 48/48 headless + typecheck + browser smoke green. Watch-outs: stored blob carries a `v` field but has no migration beyond key-narrowing (bump the localStorage key on a breaking shape change); scale ratios still PERSIST (only RESET is guarded) so New on an empty scene adopts the last working ratio. Commits 5d67f89, 0f15fe7.
- **Per-object scale ratio — core (2026-06-16):** scale moved from a single global `workingRatio` to a PER-OBJECT `ratio` (spec `docs/superpowers/specs/2026-06-16-per-object-ratio-design.md`, plan `…/plans/2026-06-16-per-object-ratio.md`). The reframe: per-object `ratio` IS the old global workingRatio promoted to object scope; `modelRatio` keeps its authoring-anchor role; the import formula `unit × modelRatio / ratio` is unchanged. Landed: (1) **ScaleMath** `objectRatio`/`objectScaleFromObject`/`exportRatiosFromState`. (2) **Import** seeds each object's `ratio = modelRatio` (`AssetLoader._registerInstantiatedMeshes` + both import/instantiate call sites); `ImportNormalizer.importScaleFactor(unit, modelRatio, ratio=modelRatio)` ⇒ seed cancels, fresh import lands at authored size. (3) **`RescaleObjectCommand`** (ScaleCommands) — RescaleWorld's vertex bake scoped to a selection: bakes `prev/next` into each object's OWN geometry, scales only its OWN local position (never shared ancestors — finding D), one undoable batch. (4) **Geometry-sharing fix (finding C):** `cloneMeshAsNewObject` now `makeGeometryUnique()` so a duplicate's per-object bake can't corrupt the source through Babylon's shared `Geometry` (latent today since RescaleWorld bakes uniformly). (5) **Export** `PrintScale.exportFactor` = `(activeObjectRatio / exportRatios[0]) × 1000` — uniform active-object factor preserves WYSIWYG for same- and mixed-ratio selections; suffix/dims follow. (6) **Bed validation REMOVED (finding G):** `MeshValidator._checkExceedsBed` + the `exceedsBed` result deleted (bed/grid/camera visuals stay). (7) **Persistence:** per-object `ratio` persisted; MIGRATION of pre-redesign `.mixo` folds old global `workingRatio` → each object's `ratio` (geometry already baked at modelRatio/workingRatio ⇒ no rebake), `targetRatio` → `exportRatios:[old]`. (8) **UI:** Properties ▸ Transform ▸ **Ratio** dropdown (presets + free-form, multi-select shows `—`, applies via `RescaleObjectCommand` expanded to logical-object parts — finding E); Print ▸ Scale tab drops the global Scene-Scale field, Print-Scale becomes an editable **target-ratio LIST** (add/remove chips). i18n keys in all 3 locales. **Deviations from spec (documented):** legacy `print.workingRatio`/`targetRatio` fields kept DEFINED in default-settings/state (settings-store + state-shape tests assert them; avoids NaN from any missed read) — they're now unused by export logic. **DEFERRED:** export still emits a SINGLE file using `exportRatios[0]` (the active target); batch-one-file-per-target needs the export/packaging loop + export-test updates — the list is stored + editable now, batch loop is the next increment. New `tests/per-object-ratio.test.mjs` (5 asserts); `export.test.mjs` setScene mirrors the global onto each object's `ratio`. 73 headless + i18n:check + typecheck + build + browser export smoke all green.
- **Per-object ratio — legacy removal + batch export (2026-06-16, follow-up):** finished the redesign cleanly. (1) **Batch export DONE** (was deferred): `_runExport` now loops `print.exportRatios`, calling extracted `_runExportForTarget` once per target with `PrintScale.setExportTargetOverride(T)` pinning that ratio for factor + suffix + filename; one scaled file per target (progress prefixed `[i/n]`). `export.test.mjs` pins `[72,144] → Test_r72to72.zip + Test_r72to144.zip` (two outputs). (2) **Legacy fields REMOVED:** `print.workingRatio`/`targetRatio` deleted from `default-settings.json` + state; the only surviving reads are `PersistenceManager` MIGRATION of pre-redesign `.mixo` (reads the loaded doc to seed per-object `ratio` + `exportRatios`). (3) **`RescaleWorldCommand` + `_applyWorldRescale` DELETED** (global rescale is the old model) — `_fmtRatioLabel` kept for `RescaleObjectCommand`; `SceneManager` import dropped from ScaleCommands; the M13 hygiene cursor-scaling-on-world-rescale test removed (moot). (4) **`SettingsStore.SCENE_PROTECTED` → `{}`** (per-object ratio is content, never a persisted setting, so reset can't touch it); settings-store + state-shape + browser-smoke tests rewritten to assert `exportRatios` resets normally instead of the old ratio-guard. Dead `push`/`RescaleWorldCommand` import dropped from PrintPanel. 72 headless + i18n:check + typecheck + build + Chrome export smoke all green.
- **Logo → inline SVG (2026-06-14, follow-up):** the header logo was `public/logo.svg` via `<img src="/logo.svg">`, which works dev+build but assumes Vite `base: '/'` (breaks under a sub-path host). Switched `#app-logo` to an INLINE SVG wordmark in `index.html` — no HTTP request, no base-path dependency, and `fill: currentColor` (sized `#app-logo svg { height: 26px }`) themes with the header text. Deleted `public/logo.svg`; browser-smoke logo check now asserts `#app-logo svg` renders (was the `src` regex). typecheck + 48 unit + browser smoke green.
- **Per-object ratio — restore fix + "as shown" default export (2026-06-16, follow-up):** (1) **Restore bug FIXED (audit #1, proven via a new browser-smoke ratio round-trip).** `restoreContainer` reloads RAW source bytes with no import normalization, and the import/ratio bake lives in vertices (scaling=1) so the saved node transform never carried it — round-trip showed `imported=0.02 → ratio 0.01 → restored=20` (raw, 2000x off). `_loadProject` now re-bakes `importScaleFactor(sourceUnit, modelRatio, ratio)` per restored mesh; size + per-object ratio survive reload for fresh AND migrated saves. Exposed a pre-existing gap (import unit scale was also lost on reload). glTF RH→LH-flip-on-restore left as a separate unverified item (STL round-trip clean). (2) **Export model corrected to "as shown".** Reference = ACTIVE object ratio; factor `(activeRatio/target)×1000`; target==activeRatio ⇒ factor 1000 ⇒ prints the displayed size. Empty `exportRatios` (new default) ⇒ `resolveExportTargets` → `[activeRatio]` = as-shown; adding absolute targets rescales relative to the object (1:144 on a 1:72 object = half). A brief "scene-relative 1000/T" attempt was reverted (it dropped the object ratio). Print panel shows an "As shown (1:R)" chip when the list is empty, add/remove absolute targets otherwise. (3) **Confirmed the export-ratio LIST exists** (add/remove chips → `exportRatios`, one file per target via the `_runExport` loop). New export tests: as-shown (empty → factor 1000, `_r72to72`) + batch (`[72,144]` → two files). 72 headless + i18n + typecheck + build + browser smoke + export smoke all green.
- **Restore re-uses fresh-import normalization — glTF flip fix (2026-06-16, follow-up):** the earlier restore fix re-baked only a uniform SCALE, which reproduced size but NOT the glTF RH→LH flip (the import bakes the flip into vertices; a raw reload + saved node transform cannot carry a reflection). Now `restoreContainer` re-runs the SAME `bakeImportTransform` as a fresh import (unit + flip + winding, at the modelRatio seed) and `_loadProject` bakes only the per-object ratio DELTA (`modelRatio/ratio`). Restore is byte-identical to a fresh import plus the saved placement. New browser-smoke round-trip imports a minimal asymmetric glTF triangle and asserts the restored world AABB == the fresh-import AABB (RH→LH flip survives); the STL ratio round-trip still guards per-object size. typecheck + 72 headless + build + browser smoke + export smoke all green.
- **Export reference-ratio refactor (2026-06-16, user edit):** export factor reference moved from raw `state.selection.activeId` to `_exportReferenceRatio(printUnits, state)` — the active object's ratio if it is in the export set, else the first printable unit — threaded as `ctx.referenceRatio` through new `exportFactorFor(ctx)` + `sceneScaleFor(state, ctx)` so factor AND filename suffix use one consistent reference across the batch. Restore re-bake extracted to `_applyPersistedRatioBake` (keeps the shared-mesh `rebaked` guard + adds `refreshBoundingInfo`). `RescaleObjectCommand._rescaleObject` now dispatches `OBJECT_UPDATED` after the state commit. Known edge (documented): the as-shown *target* still uses the selection-active ratio while the factor *reference* uses the print-set ratio — they diverge only when the active object is excluded from the export set. 73 headless + typecheck + i18n + build + browser smoke + export smoke green; Blueprint/spec/memory updated to match.
- **Remove Apply Scale/Rotation baking — user transforms never bake (2026-06-16):** the Blender-style "Apply Rotation/Apply Scale" buttons (`BakeTransformCommand`) baked a user transform into vertices and zeroed the node, which (a) destroyed the editable, persisted Scale/Rotation properties and (b) was lost on `.mixo` reload (the file stores raw bytes + the now-identity node transform; the baked vertices are in neither) — verified: 20 mm → Apply 2× → 40 mm → reload → 20 mm. REMOVED the two buttons (PropertiesPanel) + `BakeTransformCommand` (TransformCommands) + the dead `applyDis`/wiring. User rotation/scale now ALWAYS live on the node (`TransformCommand`) — editable in Properties and saved via `_decompose` — so they survive reload; export still flattens the world matrix so printed geometry is unchanged. Locked principle: system transforms (import unit/flip, ratio) bake + replay on restore; user transforms stay on the node, never baked. Browser-smoke ratio round-trip extended to also set a node scale and assert BOTH the baked ratio and the node scale survive reload. typecheck + 73 headless + build + browser smoke + export smoke + i18n green. Blueprint/memory updated.
- **Multi-shader validation: weld the whole object, not per material part (2026-06-16):** Print-tab validation reported false non-manifold (and missed real combined holes) for multi-shader models. The logical-object layer grouped material parts ONLY by `sourceGroupId` (set only when a single MultiMaterial mesh is split on import), but most multi-shader models export as a glTF MULTI-PRIMITIVE mesh (one mesh per material) — never split → no `sourceGroupId` → each part validated alone → every material seam looked like a hole. A browser probe confirmed Babylon loads multi-primitive meshes as children named `<stem>_primitive<N>` sharing a parentId, while separate objects are distinct nodes with neither. Fix: (1) `_registerInstantiatedMeshes` now also assigns a shared `logicalObjectId` to glTF multi-primitive meshes (key `pp:<parentId>:<stem>` from the `_primitive` name), so the whole logical layer (Outliner one-entry, selection, ratio, export weld) treats a multi-shader model as one object; (2) `MeshValidator.validateMesh` welds by `logicalObjectPartIds` (>1 part ⇒ union topology) instead of the `sourceGroupId`-only branch. Safe against wrong-merge: needs the `_primitive` name AND shared parent, which a collection of separate objects lacks. New browser-smoke regression: closed 2-material cube → 1 logical object, union watertight (0 non-manifold); two separate objects → stay 2, not merged. typecheck + 73 headless + build + browser smoke + export smoke green. Blueprint §0.1/§9 + memory updated.
- **Audit remediation wave (2026-06-16):** fixed 14 of the 44 audit findings (docs/reviews/2026-06-16-full-audit.md) — all this-session-introduced regressions plus the bounded mediums. R1: `_remapDuplicateLogicalObjects` now relinks the logicalObjectId-only (glTF multi-primitive) case, not just sourceGroupId, so duplicating a multi-shader object no longer splits it (regression test in logical-objects.test). R2/R3: `print` SettingsStore SCHEMA narrowed to printer prefs so per-project `exportRatios` no longer leaks to per-user localStorage / across New (settings-store + browser-smoke assert it is content, preserved through reset). R4: `relinkAsset` threads the `_reBaked` WeakSet (no double-bake). R5: ShaderPanel Assign expands `logicalObjectCommandIds` (shades all parts). R6: `SelectionOutline.setActive` accepts a list; all parts of a multi-primitive active object get the active tint. R7: `followActive` unions all part bounds (event-cached part ids, no per-frame getState). R8: scale-tab example dims use the active object (mesh + reference ratio agree). M2: validation invalidation-sequence prevents a stale-mark from being clobbered by an in-flight worker result. M3: unlit shaders drive `emissiveColor` from the chosen colour (no more white viewport/Ke). M4: imported texture UV transform harvested into uvBase. M6: `CameraRig.suspendFollow` neutralises follow-mode during offline render + pose capture (turntable no longer reframes vs preview). M8: G/R/S modal honours pivotMode (world/cursor/active). M9: printPreview/baseColor modes guard to inspectable content meshes (no bed/section/back-face material bleed). Deferred with designs (risk of new bugs if rushed): H1 (duplicate collapse on reload — needs per-duplicate assetId+bytes), M1 (autoFix not persisted), M5 (SmartReplace multi-part), M7 (autosave loose-drop ghost), M10 (section-clone follow), + all lows. typecheck + 74 headless + build + i18n + browser smoke + export smoke green.
- **Audit H1 dup-reload — second bug (mid-bounce save) + M5 SmartReplace gate (2026-06-16, follow-up):** the H1 duplicate-collapse fix (`fb3ce30`, per-object clone-on-restore) was correct, but its browser-smoke round-trip exposed a SEPARATE, pre-existing bug: a duplicate saved DURING its bounce-in pop reloaded permanently shrunk (~0.68×, frame-timing dependent). Root cause: `ImportBounce` animates the mesh's LIVE node scaling (`scaling = orig × f`), and `_buildDocument` evaluates `assetLibrary: await …` BEFORE the synchronous `sceneObjects: _serialiseSceneObjects()`, so render frames tick during the async asset-encode and `_decompose` captures the world matrix mid-pop — baking the transient `f` into the saved transform; on reload `_applyWorld` freezes it. `cloneMeshAsNewObject` dispatches `ASSET_INSTANTIATED`, so duplicates bounce too → the test (dup → rescale → immediate save) hit it every run. Fix: `ImportBounce.settleImportBounce()` snaps every in-flight bounce to its resting `orig` and stops the observer; `_buildDocument` calls it before capturing any transform, so saved state always reflects resting scale (detaching the pivot post-drift would have baked it in — setParent preserves world). Header comment corrected (the "never touches state" invariant only holds AFTER settle). Also closed **M5** — `_smartReplace` (ContextMenu) now refuses with a toast when the active object or any target is a multi-part logical object (`logicalObjectPartIds > 1`), since SmartReplaceCommand clones only the lead and would lose sibling parts / orphan the targets' non-lead parts; full multi-part replace is the eventual fix. New i18n key `toast.smartReplaceMultiPart` (en/ja/zh-Hant). typecheck + 74 headless + build + browser smoke + export smoke green.
- **Audit M10 — cross-section fill follows live moves (2026-06-16, follow-up):** the amber back-face fill clone (`ViewEffects._ensureCapClone`) baked the source world matrix once at clone time, so moving the model while a cut was active detached the fill from the cut (the border, derived from content bounds, also went stale). Fix: the clone now copies the source's world matrix into its own (unparented) position/rotationQuaternion/scaling every frame inside its existing `onBeforeRenderObservable` (reusable scratch vectors, no per-frame allocation) — it follows through gizmo drags, body drags and undo. `initViewEffects` subscribes `TRANSFORM_COMMITTED` and replays `_updateSectionViz(_lastAxis,_lastOff)` (cached on each apply) to rebuild the bounds-based border + reconcile the clone set, preserving a hidden-during-capture viz state. Clone stays `parent=null`/no-meshId so the existing ancestor-walk exclusion from clip/shadow/mask/export is untouched. Browser-smoke gains `capFollows` (move the source, render, assert the clone's world X tracks). typecheck + 74 headless + build + browser smoke + export smoke green.
- **Audit M7 — autosave embeds handle-less loose drops (2026-06-16, follow-up):** autosave passes `skipEmbed` (A9: re-base64ing every asset each 60s froze big scenes), recovering via the live tiers (dir / hash-scan / file handle). But a LOOSE drag-drop has no dir/file handle, so on the non-Chrome / revoked-permission / fallback paths its embedded bytes are the ONLY recovery tier — skipping them turned it into a ghost on crash-recovery. Fix: `_serialiseAssetLibrary` now embeds even on autosave when an asset has no live tier (`!directoryHandleKey && !fileHandleKey`); handle-backed assets still skip (A9 perf preserved) and container-owned textures (`kind==='texture' && isImported`) still never carry standalone bytes. The A9 unit test was the loose-drop case asserting the OLD (ghosting) behaviour — rewritten as `A9/M7` with two assets: a handle-backed one (autosave skips bytes, keeps hash) and a handle-less loose drop (autosave embeds). typecheck + 74 headless + build green.
- **Audit M1 — auto-fix geometry edits survive reload (2026-06-16, follow-up):** the Print-tab "Auto-Fix" button (`MeshValidator.autoFix`) welds (MergeByDistance) / flips inverted-normal indices on the LIVE mesh, but `.mixo` keeps only raw source bytes + ratio — so a reload rebuilt the mesh with its original defects and the fix vanished. Fix: `autoFix` refactored to a shared `applyGeometryFix(mesh, type)` (used by both the button and replay); the PrintPanel handler records the applied fix types onto the SceneObject (`geometryFixes` set); persistence serializes `geometryFixes` and `_loadProject` replays them via `MeshValidator.replayGeometryFixes(mesh, types)` AFTER the per-object ratio bake — at the displayed scale so the weld's absolute `MERGE_DISTANCE` behaves identically to when first applied. Each restored SceneObject owns a distinct mesh (duplicates are cloned), so replay runs once per object. Browser-smoke round-trip: flip a live STL's winding, tag + save + reload, assert the doc carried `geometryFixes:['invertedNormals']` and the restored indices match the flipped winding. With this, the originally-deferred audit batch (H1, M1, M5, M7, M10) is fully closed — only LOW findings remain. typecheck + 74 headless + build + browser smoke + export smoke green.
- **Audit LOW sweep wave 1 (2026-06-16):** verified each reviewer-reported LOW before touching it. FIXED: (a) #13 — removed dead MediaRecorder-era `turntableAlpha` + `pickVideoFormat` from RenderMath (production computes the camera delta inline from `turntableProgress`; video is WebCodecs-only since 2026-06-13) + their now-orphaned tests; (b) #18 — `_printPartsHaveWarnings` now optional-chains `e.results?.some(...)` (a cache entry without a results array would have thrown); (c) #4 — `restoreContainer` gained a `catch` mirroring the live-import cleanup (revoke source blob URL + OBJ sibling URLs on a failed restore; the caller ghosts the asset and never revoked them otherwise). SKIPPED with reason (reviewer flags that don't hold up): #14 (`angularSensibility` writes are LIVE — radius-scaled orbit feel in `_applyFollowTarget`, not dead), #11 (mask renderList is rebuilt on every selection change so a mesh can't linger disposed in normal flow; a per-add dispose observer would itself leak observers across the frequent rebuilds), #7 (`_buildGroupUnion` `Uint32Array` overflow needs >4 billion verts — physically unreachable). typecheck + 72 headless (−2 dead-code tests) + build green.
- **Audit LOW sweep wave 2 + triage (2026-06-16):** added the #24 invariant pin (`tests/printer-profile.test.mjs` asserts `default-settings.json` bedDimensions equals `bedDimensionsForPrinter(targetPrinterId)` — they reset together on New, so config drift would silently make the bed preview lie). Completed the LOW triage by verifying each remaining flag against the code (verdicts recorded in the audit doc's "LOW triage verdicts" section): several reviewer flags do NOT hold — `_resolveAssetBlob` tier-1 path-trust is the intended live-link/edit-in-place contract (contentHash is the tier-2 relink, not a tier-1 gate); ViewportToggles needn't re-push overlays on load because `_loadProject` already re-applies every overlay via `setOverlay`; #14/#11/#7 as noted in wave 1. Remaining open LOWs are perf items needing a measured design (main-thread save base64, O(M×S) dedupe, Properties rebuild, BoxSelect Ray alloc) or genuinely minor edges (ghost-part ratio no-op, cursor visibility on load, `.dup` collision at 999+, documented 3MF re-import scale) — left tracked, not rushed. All HIGH+MEDIUM remain closed. typecheck + 72 headless + build green.
- **Audit LOW sweep wave 3 (2026-06-16):** two more verified fixes. #16 (Mimaki full-res-lock) — `_cloneMaterialWithTexture` now explicitly copies a cloned texture's `metadata` (`{ ...t.metadata }`) because Babylon's `Texture.clone()` doesn't reliably carry custom metadata; without it a UV-override clone would lose `metadata.mixoAssetId`, and export (`ExportTextures.getAssetIdForTexture`) would miss its tier-1 source-byte resolution and fall back to GPU readback (downscaled) — breaking the full-res export lock. #8 — `ThreeMFLoader._buildContainer` tracks `usedTexIds` and revokes the blob URLs of any `texture2d` part no built object referenced (used textures keep their URL alive for the live Babylon texture; the `!made` throw path leaves `usedTexIds` empty so all are revoked). Also recorded that #21's premise is wrong: BoxSelect's `getForwardRay` alloc is per-mesh at the single `end()` resolve, not per pointermove (`update()` only redraws the marquee div) — one-shot, left as-is. typecheck + 72 headless + build + Chrome export round-trip green. Remaining open LOWs are perf items needing measured design + minor edges; all HIGH+MEDIUM stay closed.
- **Audit LOW sweep wave 4 (2026-06-16):** #22 — cursor visibility now restored on load. Visibility tracks `pivotMode==='cursor'` (set on every `setPivotMode`/interaction) but `_loadProject` restored only the cursor POSITION via `setCursorFromState`; now it also calls `SceneManager.setCursorVisible(getState().selection.pivotMode === 'cursor')` after selection is restored, so a project saved in cursor-pivot mode reopens with the 3D cursor showing. Verified the remaining LOWs are not worth a risky change: #6 (group-cache stale siblings) is an ungroup-only edge on a non-blocking badge — deletes already `_dropEntry` and the M2 seq machinery handles the real in-flight races; #1 (per-object `sourceUnit` not serialized) is latent because `asset.sourceUnit` is the authoritative serialized source. Genuinely open after this wave: perf items needing measurement (#3 main-thread save base64, #15 O(M×S) dedupe, #17 Properties rebuild, `_waitReady` cap) + negligible edges (#5,#9,#10,#20,#23). typecheck + browser smoke green; all HIGH+MEDIUM + the actionable LOWs now closed.
- **Audit LOW sweep wave 5 (2026-06-16):** #10 — solid-colour OBJ export now NaN-alpha-safe. The MTL `d`/`Tr` lines used `Math.max(0,Math.min(1,Number(mat.alpha ?? 1)))`, which yields `NaN` for a NaN alpha (the `?? 1` doesn't catch NaN, and `Math.max(0,NaN)` is NaN), while the synthesized PNG α byte went through `clamp255(mat.alpha ?? 1)` → also `NaN` → a literal "NaN" in the filename hex. New shared `_safeAlpha01` (non-finite ⇒ opaque 1) drives BOTH so they always agree and never emit "NaN"; headless regression test asserts a NaN-alpha mesh exports `solid_FFFFFFFF.png` + `d 1.0000` with no "NaN" anywhere in the MTL. Also recorded the #15 (shader-dedupe O(M×S)) finding: attempted a TDD guard first but the headless env's mock materials don't satisfy `_detectType`'s `instanceof` checks (PBR variant classes absent), so a safe refactor needs a browser fixture or benchmark — exploration reverted, left deferred with that concrete reason. typecheck + 72 headless + build green. Audit status: all HIGH+MEDIUM + every cheap/clearly-correct LOW now fixed; remaining open LOWs are perf items needing measurement (#3,#15,#17,_waitReady) + truly-minor edges (#1,#5,#6,#9,#20,#23).
- **Audit LOW perf measurement (2026-06-16):** converted the two "needs measurement" perf LOWs into data-backed decisions via a headless micro-benchmark (no repo file — inline node bench of the real algorithms). #15 (shader dedupe O(M×S)): the actual signature scan is 1.1 ms at M=50/S=200, 13 ms at M=200/S=500, and only 78 ms at an implausible M=500/S=1000 simultaneous import — negligible at any scale this tool sees; the incremental-Map refactor (0.2–1.1 ms) isn't worth its behavior-sensitivity + the headless-guard impossibility, so **reclassified won't-fix**. #3 (manual-save base64): **confirmed real** — 41/193/402 ms for a single 10/50/100 MB asset, synchronous on the main thread — but only on manual save / explicit .mixo embed (autosave skips via A9) and the per-asset `await` already makes multi-asset saves yield between assets, so the residual is a single huge asset whose only real fix is a Web Worker offload (architectural, scoped — justified by the data, deferred as a distinct task). Audit doc updated with the numbers. Every one of the 44 findings is now fixed, verified-not-a-bug, or deferred/won't-fix with a concrete (now partly measured) reason.
- **Audit LOW #17 verified non-issue (2026-06-16):** the Properties panel's full `innerHTML` rebuild is subscribed to `TRANSFORM_COMMITTED`, which fires at drag-END / nudge (`PivotSession._onGizmoDragEnd`), NOT per frame; and `renderShaderPreview` emits a cheap CSS swatch or a PRE-BAKED thumbnail data-URL (made once at import by `_generateThumbnailFor`, never re-rendered). A once-per-commit rebuild of cheap strings is the standard property-panel pattern — the audit's "incl. shader previews" worry doesn't hold. Reclassified won't-fix. After this, every one of the 44 audit findings is fixed, verified-not-a-bug (with code evidence), measured (#3/#15), or won't-fix-with-reason — the ONLY item left genuinely open is `_waitReady`'s 2 s capture cap, which can't be retuned safely without a real heavy-asset repro.
- **Audit LOW `_waitReady` cap fixed (2026-06-16):** the offline-capture readiness wait already polls a REAL signal (`texture.isReadyForRendering() && camera.isReady(true)`), so the ceiling only bounds a genuinely-stuck capture — but the 2 s cap (200 × 10 ms) was too short for the heavy 4096²+/high-poly print scenes this tool targets (textures still uploading past 2 s), spuriously failing the PNG/turntable capture. Raised to 10 s via named `_READY_TIMEOUT_MS`/`_READY_POLL_MS` + a clearer timeout message. Strictly an improvement: a fast scene still returns immediately (browser-smoke `capturePng` confirms — no regression), and a never-ready capture still errors rather than hangs, so no heavy-asset repro is needed to validate it. **This closes the last item that was repro-blocked.** The ONLY finding now left open is #3 (manual-save base64) — measured real but its only fix (a Web Worker on the persistence-critical save path) carries project-corruption (HIGH) risk to shave a sub-second freeze on an infrequent manual action (LOW); deliberately left as a user-greenlit scoped task, not a sweep-pressure patch. typecheck + browser smoke + build green.
- **Audit #3 FIXED — manual-save base64 offloaded to a Web Worker (2026-06-16):** the last open audit finding. Measured first (headless bench: 41/193/402 ms for a single 10/50/100 MB asset, synchronous, blocking the main thread on manual save / explicit .mixo embed; autosave already skips embedding via A9). Implemented carefully on the persistence-critical / Mimaki-embed save path: (1) `src/core/workers/base64Codec.js` — one shared pure `encodeBase64` feeding BOTH the worker and the sync fallback, so output is byte-identical by construction; (2) `src/core/workers/Base64.worker.js` — module worker running that codec; (3) `src/core/Base64Worker.js` — `encodeBase64Async` COPIES the bytes and transfers the copy (the live buffer is never detached), falls back to the sync codec when `Worker` is absent (headless) or the worker errors; (4) `_serialiseAssetLibrary` awaits `encodeBase64Async`, `_b64FromBuf` delegates to the shared codec. Main-thread blocking on a heavy save drops from the full btoa (~400 ms/100 MB) to just a memcpy; the encode runs off-thread. Guards: STANDARD-base64 reference test + async/sync byte-identity test (tests/persistence.test.mjs, sync fallback headless) + an EXPLICIT browser-smoke pin running `encodeBase64Async` through the REAL Chrome worker across the 0x8000 chunk boundary, asserting it equals `_b64FromBuf` and never detaches the source; the existing `_buildDocument`→`_loadProject` round-trips also embed/decode via the real worker (any byte mismatch fails the reload). typecheck + 72 headless + build + browser smoke + export smoke all green. **ALL 44 audit findings are now fixed, verified-not-a-bug, or won't-fix-with-reason — none open.** NOTE: this finding was also spawned as a background task chip earlier in the session and the user STARTED it in a separate worktree before this in-branch implementation landed — the two need reconciling (keep one, discard the other).
